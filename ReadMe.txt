Setting up the project
----------------------
1. After unzipping the project, please place that in respective php server folder (Ex: Wamp(www), Lamp(www/html), Xamp(htdocs))
2. Please change the constant values in config.php file placed in uploadapi folder (Ex: Database constants)
3. Run the project using the URL http://localhost/imageupload
